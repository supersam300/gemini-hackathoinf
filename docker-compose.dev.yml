version: '3.8'

services:
  # Development web application with hot reload
  web-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_namein.
    
    running wslexec: The operation timed out because a response was not received from the virtual machine or container. Wsl/Service/RegisterDistro/CreateVm/HCS_E_CONNECTION_TIMEOUT: c:\windows\system32\wsl.exe --import-in-place docker-desktop <home>\appdata\local\docker\wsl\main\ext4.vhdx: exit status 0xffffffff (stderr: , wslErrorCode: Wsl/Service/RegisterDistro/CreateVm/HCS_E_CONNECTION_TIMEOUT): gemini-hackathon-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      VITE_HOST: 0.0.0.0
    restart: unless-stopped
    networks:
      - gemini-network-dev
    stdin_open: true
    tty: true

networks:
  gemini-network-dev:
    driver: bridge
